//This express.js file came from the resource folder provided from Populi

//const express = require('express'); --This is in app.js code
//const handlebars = require('express-handlebars'); --Installed npm i express-handlebars
//const bodyParser = require('body-parser');  --Express now has body parser as a dependency so it is unnecessary to require it 

//const app = express(); --This is in app.js code

    //TODO: Setup the view engine --

    //TODO: Setup the body parser --

    //TODO: Setup the static files --

//module.exports = app(); --This is in app.js code

