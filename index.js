//This file is from resources folder from Populi
//When npx Express Generator was installed, it created a bin folder with a www.js file which created a server and set the port number to 3000.  The code below is no longer needed, therefore this index.js file is no longer needed for this project set up as well



// const env = process.env.NODE_ENV || 'development';

// const config = require('./config/config')[env];
// const app = require('express')();

// require('./config/express')(app);
// require('./config/routes')(app);

// app.listen(config.port, console.log(`Listening on port ${config.port}! Now its up to you...`));